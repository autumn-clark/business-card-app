import 'package:flutter/material.dart';

class RecentConnects extends StatelessWidget {
  const RecentConnects({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('recent'));
  }
}