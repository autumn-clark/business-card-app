import 'package:flutter/material.dart';

class MapSpinnet extends StatelessWidget {
  const MapSpinnet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Map"),
    );
  }
}